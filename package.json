{
  "name": "avatar-chatbot",
  "version": "1.0.0",
  "description": "Customizable avatar chatbot with Claude AI and speech synthesis",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}